[build]
buildCommand = "/mise/installs/python/3.13.13/bin/pip install -r requirements.txt"

[deploy]
startCommand = "/mise/installs/python/3.13.13/bin/uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
