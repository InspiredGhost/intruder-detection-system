# Minimal requirements for Raspberry Pi edge detector
# face_recognition and dlib are installed via apt (system packages)
# torch is NOT needed — only face_recognition runs on Pi
opencv-python-headless>=4.8.0
numpy>=1.24.0
httpx>=0.24.0
sounddevice>=0.4.6
soundfile>=0.12.1
Pillow>=10.0.0
